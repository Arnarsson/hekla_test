{
  "name": "hekla-setup-alpha",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run pipeline.ts",
    "test": "bun test",
    "lint": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
